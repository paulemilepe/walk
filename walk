body {
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
}

.header {
  position: relative;
  height: 100vh;
  background-color: #f0f0f0;
  text-align: center;
}

.title {
  color: black;
  font-size: 3em;
  margin-top: 30vh;
  position: relative;
  z-index: 1;
}

.subtitle {
  color: black;
  font-size: 1em;
  margin-top: 10px;
  position: relative;
  z-index: 1;
}

.mountains {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url('chemin/vers/une/image-de-montagnes.png');
  background-size: cover;
  background-position: center;
  animation: moveMountains 5s infinite;
}

@keyframes moveMountains {
  0% {
    transform: translateY(0);
  }
  100% {
    transform: translateY(-100%);
  }
}
